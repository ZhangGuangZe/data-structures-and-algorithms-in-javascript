import { Node } from '../linked-list/Node.js'

export class CircularLinkedList {
  constructor() {
    this.head = null;
  }
  /**
   * 指定位置插入节点
   *
   * @param {*} target
   * @param {*} val
   * @memberof CircularLinkedList
   */
  insert(target, val) {
    
  }
  /**
   * 删除指定节点
   *
   * @param {*} val
   * @memberof CircularLinkedList
   */
  remove(val) {

  }
}